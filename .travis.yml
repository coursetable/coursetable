language: node_js

services:
  - docker

before_install:
- docker pull php
- docker run -v `pwd`:/app -w /app php /bin/sh -c "php composer.phar install"

script:
- docker run -v `pwd`:/app -w /app php /bin/sh -c "./phpcs.sh"
